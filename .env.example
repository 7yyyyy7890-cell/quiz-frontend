# Leave empty in Docker — Vite proxies /api and /ws to the backend service.
# For Netlify / standalone builds, set your deployed API:
# VITE_API_URL=https://api.example.com
# VITE_WS_URL=wss://api.example.com

VITE_API_URL=
VITE_WS_URL=
